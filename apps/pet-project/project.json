{
  "name": "pet-project",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/pet-project/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/pet-project",
        "main": "apps/pet-project/src/main.ts",
        "tsConfig": "apps/pet-project/tsconfig.app.json",
        "assets": ["apps/pet-project/src/assets"],
        "webpackConfig": "apps/pet-project/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "pet-project:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "pet-project:build:development"
        },
        "production": {
          "buildTarget": "pet-project:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/pet-project/jest.config.ts"
      }
    }
  },
  "tags": []
}
